# 🚀 Пошаговое руководство по запуску

Полная инструкция от установки до первого звонка.

---

## 📦 ШАГ 1: Установка

### 1.1. Клонируйте проект

```bash
cd telegram-auto-caller
```

### 1.2. Установите зависимости

```bash
npm install
```

### 1.3. Установите браузер Chromium

```bash
npm run install:browsers
```

---

## 🎙️ ШАГ 2: Настройте Voice AI (опционально)

### Вариант A: Используйте ваш Whisper + OXXN стек

Создайте HTTP API сервер:

```javascript
// voice-ai-server.js
const express = require('express');
const app = express();

app.post('/api/transcribe', async (req, res) => {
    // 1. Получите аудио из req.body
    // 2. Запустите Whisper (guff) для транскрипции
    // 3. Отправьте на OXXN Realtime API для интонаций
    // 4. Верните результат
    
    res.json({
        text: "да, интересно",  // Обязательное поле
        sentiment: "positive",   // Опционально
        emotions: {...}          // Опционально
    });
});

app.listen(3000);
```

**Подробная инструкция:** [VOICE_AI_INTEGRATION.md](VOICE_AI_INTEGRATION.md)

### Вариант B: Используйте заглушку для тестирования

Если Voice AI ещё не готов - система будет использовать случайные ответы для тестирования.

---

## 📋 ШАГ 3: Подготовьте данные

### 3.1. Список номеров

Создайте файл `phones.txt`:

```
+380501234567
+380631234567
+380931234567
```

**Формат:**
- Каждый номер с новой строки
- E.164 формат (+код страны + номер)
- Без пробелов и дефисов

### 3.2. Аудиосообщение

Положите аудиофайл в проект:

```bash
audio-message.mp3  # Ваше аудиосообщение
```

**Поддерживаемые форматы:** MP3, WAV, OGG, M4A

**Рекомендации:**
- Длительность: 10-15 секунд
- Четкая речь без шумов
- Битрейт: 128 kbps+

---

## 🔐 ШАГ 4: Подготовьте сессию Telegram (опционально)

### Вариант A: Обычная авторизация (QR-код / SMS)

Ничего не делайте - при первом запуске откроется окно авторизации.

### Вариант B: Импорт готовой сессии (для аккаунтов без 2FA)

Положите в проект:
- `session.json` - файл сессии
- ИЛИ `tdata/` - папка Telegram Desktop

**Подробнее:** [SESSION_IMPORT.md](SESSION_IMPORT.md)

---

## 🌐 ШАГ 5: Настройте прокси (опционально)

### Один прокси

Подготовьте строку:

```
http://user:pass@proxy-host:port
```

### Несколько прокси

Создайте файл `proxy.txt`:

```
http://user1:pass1@proxy1:8080
http://user2:pass2@proxy2:8080
http://user3:pass3@proxy3:8080
```

**Система автоматически будет ротировать прокси** после N звонков.

---

## 🚀 ШАГ 6: Запуск!

### Вариант 1: Интерактивная настройка (РЕКОМЕНДУЕТСЯ)

```bash
npm run start:v3
```

**Или:**

```bash
node voice-ai-caller-v3.js
```

**Что произойдет:**

Система задаст вам вопросы:

```
📋 ШАГ 1: Список номеров для обзвона
Введите путь к файлу с номерами (по умолчанию ./phones.txt): 
✅ Загружено 3 номеров

🎵 ШАГ 2: Аудиосообщение
Введите путь к аудиофайлу (по умолчанию ./audio-message.mp3): 
✅ Аудиофайл найден

⏱️  ШАГ 3: Таймаут ожидания ответа
Сколько секунд ждать ответа? (по умолчанию 20): 15
✅ Таймаут установлен: 15 секунд

⏳ ШАГ 4: Задержка между звонками
Задержка между звонками в секундах? (по умолчанию 3): 5
✅ Задержка установлена: 5 секунд

🌐 ШАГ 5: Настройка прокси
Использовать прокси? (y/n, по умолчанию n): y
Введите прокси или путь к proxy.txt: proxy.txt
✅ Загружено 3 прокси из файла
Менять прокси после скольких звонков? (по умолчанию 50): 20
✅ Прокси будет меняться каждые 20 звонков

🔐 ШАГ 6: Авторизация Telegram
Импортировать сессию из файла? (y/n, по умолчанию n): y
Путь к session.json или tdata папке: ./session.json
✅ Сессия будет импортирована

🤖 ШАГ 7: Проверка через @SpamBot
Проверить аккаунт через @SpamBot после работы? (y/n, по умолчанию y): y
✅ Проверка включена

🎙️  ШАГ 8: Voice AI для распознавания речи
Использовать Voice AI API? (y/n, по умолчанию n): y
Введите URL Voice AI API: http://localhost:3000/api/transcribe
✅ Voice AI настроен

📊 ИТОГОВАЯ КОНФИГУРАЦИЯ
══════════════════════════════════════════════════════════
📋 Номеров: 3
🎵 Аудио: ./audio-message.mp3
⏱️  Таймаут ответа: 15 сек
⏳ Задержка между звонками: 5 сек
🌐 Прокси: 3 шт (ротация каждые 20 звонков)
🔐 Сессия: импорт
🤖 SpamBot проверка: да
🎙️  Voice AI: настроен
══════════════════════════════════════════════════════════

Всё верно? Начать обзвон? (y/n): y

🚀 Запускаем обзвон...
```

---

### Вариант 2: Программная настройка

Создайте свой скрипт:

```javascript
const VoiceAICallerV3 = require('./voice-ai-caller-v3');

const caller = new VoiceAICallerV3({
    phoneListFile: './phones.txt',
    audioMessageFile: './audio-message.mp3',
    callTimeout: 15000,              // 15 секунд
    delayBetweenCalls: 5000,         // 5 секунд
    
    // Прокси
    proxyServer: 'http://proxy:8080',
    proxyUsername: 'user',
    proxyPassword: 'pass',
    callsBeforeProxyRotation: 20,
    
    // Сессия
    importSessionPath: './session.json',
    
    // Voice AI
    voiceAIEndpoint: 'http://localhost:3000/api/transcribe',
    
    // SpamBot
    checkSpamBotAfter: true
});

// Пропускаем интерактивную настройку
caller.interactiveSetup = async () => {
    console.log('✅ Используется программная конфигурация');
};

caller.startCalling();
```

---

## 🎬 ШАГ 7: Процесс обзвона

После запуска вы увидите:

```
🚀 Запуск браузера...
🌐 Используется прокси: http://proxy1:8080
🌐 Открытие Web Telegram...
✅ Готов к работе!

📋 Загружено 3 номеров

[1/3] ━━━━━━━━━━━━━━━━━━━━
📞 Звоню на +380501234567...
📞 Звонок инициирован...
⏳ Ожидание ответа (15 сек)...
✅ Звонок принят!
🎵 Проигрывание аудио...
✅ Аудио проиграно
👂 Слушаю ответ...
🎤 Извлечение аудио...
🗣️  Распознавание речи...
✅ Распознано: да, интересно
💬 Ответ: "да, интересно" → yes
📴 Завершение...
✅ Завершено
⏳ Задержка 5 сек...

[2/3] ━━━━━━━━━━━━━━━━━━━━
...
```

---

## 📊 ШАГ 8: Результаты

### Автоматически создаются файлы:

**1. call-results.json** - детальные данные

```json
[
  {
    "status": "answered",
    "phoneNumber": "+380501234567",
    "answered": true,
    "response": "да, интересно",
    "response_category": "yes",
    "timestamp": "2026-09-20T14:30:00Z"
  }
]
```

**2. call-results.csv** - для Excel/Google Sheets

```csv
Номер,Статус,Ответил,Детали,Ответ,Категория,Время
+380501234567,answered,Да,answered,"да, интересно",yes,2026-09-20T14:30:00Z
```

**3. Консольная статистика**

```
══════════════════════════════════════════════════
📊 СТАТИСТИКА
══════════════════════════════════════════════════
Всего: 3
✅ Ответили: 2
👍 Заинтересованы: 1
👎 Отказались: 1
══════════════════════════════════════════════════
```

---

## 🤖 ШАГ 9: Проверка SpamBot

После обзвона автоматически проверяется статус аккаунта:

```
🤖 Проверка аккаунта через @SpamBot...
✅ Аккаунт чистый - ограничений нет

📝 Результат проверки SpamBot: clean
```

**Возможные статусы:**
- `clean` - всё хорошо, ограничений нет
- `limited` - есть ограничения
- `unknown` - не удалось проверить

---

## 🔄 ШАГ 10: Ротация прокси

При достижении лимита звонков на прокси:

```
[20/100] ━━━━━━━━━━━━━━━━━━━━
🔄 РОТАЦИЯ ПРОКСИ → http://proxy2:8080

[21/100] ━━━━━━━━━━━━━━━━━━━━
...
```

Система автоматически переключается на следующий прокси из списка.

---

## ✅ Чек-лист перед запуском

- [ ] Node.js 16+ установлен
- [ ] `npm install` выполнен
- [ ] `npm run install:browsers` выполнен
- [ ] `phones.txt` создан и заполнен
- [ ] `audio-message.mp3` добавлен
- [ ] Voice AI API запущен (если используется)
- [ ] Прокси настроены (если используются)
- [ ] Сессия Telegram готова (или будете авторизовываться)
- [ ] Прочитаны рекомендации по безопасности

---

## 🎯 Быстрый старт (минимальная конфигурация)

Для первого теста без прокси и Voice AI:

```bash
# 1. Установка
npm install
npm run install:browsers

# 2. Создайте phones.txt с 2-3 тестовыми номерами
echo "+380501234567" > phones.txt
echo "+380631234567" >> phones.txt

# 3. Добавьте audio-message.mp3

# 4. Запуск
npm run start:v3

# 5. В интерактивной настройке:
# - Прокси: n
# - Сессия: n (авторизуетесь через QR)
# - SpamBot: y
# - Voice AI: n (используется заглушка)

# 6. Подтвердите и запустите
```

---

## 🐛 Устранение проблем

### Ошибка: "Cannot find module 'playwright'"

```bash
npm install
npm run install:browsers
```

### Ошибка: "phones.txt not found"

Создайте файл и добавьте номера:

```bash
echo "+380501234567" > phones.txt
```

### Ошибка: "audio-message.mp3 not found"

Положите аудиофайл в корень проекта.

### Ошибка: "Connection refused" (Voice AI)

Убедитесь что Voice AI API запущен:

```bash
node voice-ai-server.js
# Должен быть доступен на http://localhost:3000
```

### Не работает прокси

- Проверьте формат: `http://user:pass@host:port`
- Убедитесь что прокси активен
- Попробуйте без username/password если не нужны

---

## 📚 Дополнительные материалы

- **[VOICE_AI_INTEGRATION.md](VOICE_AI_INTEGRATION.md)** - Интеграция Whisper + OXXN
- **[CALL_FLOWS.md](CALL_FLOWS.md)** - Все статусы звонков
- **[SESSION_IMPORT.md](SESSION_IMPORT.md)** - Импорт сессий
- **[CHANGELOG.md](CHANGELOG.md)** - История изменений

---

## 🎉 Готово!

Теперь у вас настроена полнофункциональная система автоматического обзвона через Telegram!

**Следующие шаги:**
1. Протестируйте на небольшом списке (2-3 номера)
2. Проверьте все статусы работают корректно
3. Настройте Voice AI для распознавания речи
4. Запустите массовый обзвон

**Удачи! 🚀📞**
